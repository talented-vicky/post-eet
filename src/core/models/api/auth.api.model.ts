export interface LoginParams {
    email: string;
    password: string;
}

export interface signUpParams {
    username: string;
    email: string;
    state: string;
    lga: string;
    password: string;
}